<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="NoteMe.Views.NoteEntryPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Name="Delete" Order="Primary" Priority="1" Command="{Binding DeleteNoteCommand}"/>
        <ToolbarItem Name="Save" Order="Primary" Priority="0" Command="{Binding SaveNoteCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <Entry Text="{Binding NoteToEdit.Title}"
                   VerticalOptions="Start" 
                   HorizontalOptions="FillAndExpand"
                   FontSize="Large" 
                   FontAttributes="Bold"
                   Margin="10"/>
            <Label FontSize="Small" Margin="10, 0, 10, 0" >
                <Label.FormattedText>
                    <FormattedString>
                        <FormattedString.Spans>
                            <Span Text="{Binding NoteToEdit.Modified, StringFormat='{0:MMMM dd, yyyy}'}"/>
                        </FormattedString.Spans>
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <Editor Text="{Binding NoteToEdit.Content}"
                    VerticalOptions="FillAndExpand"
                    HorizontalOptions="Fill"
                    Margin="10"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>